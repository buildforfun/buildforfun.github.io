# buildforfun.github.io

To test the site works locally before pushing, run the command:
```bundle exec jekyll serve```<br>

Make sure you install bundle first, if you haven't:
```bundle install```<br>
